<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bookbook.mapper.UserMapper">
    <insert id="insertUser" parameterType="SignUpUserDTO">
        insert into user (nickname, hashed_password, introduce) values(#{nickname}, #{password}, #{introduce})
    </insert>

    <select id="selectUserNickname" parameterType="java.lang.String" resultType="boolean">
        select exists(select 1 from user where nickname = #{nickname});
    </select>
</mapper>